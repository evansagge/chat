Usage
====

1. Install the gems

  ```
  bundle Install
  ```

2. Run the server:

  ```
  bundle exec rackup server.ru -E production
  ```

3. Run the clients

  ```
  bundle exec ruby client.rb [name]
  ```
